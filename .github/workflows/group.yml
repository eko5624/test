name: group
defaults:
  run:
    shell: bash
on:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: archlinux/archlinux:base-devel
    steps:  
      - name: users
        run: |
          cat /etc/passwd
          
      - name: github path
        run: |
          echo "$GITHUB_PATH"
